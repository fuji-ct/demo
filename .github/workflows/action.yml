name: List S3 Buckets

on:
  workflow_dispatch:

jobs:
  list-buckets:
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        run: |
          aws configure set aws_access_key_id "AKIAYKHCHQYXAS7VGGU5"
          aws configure set aws_secret_access_key "1boPMRiP1Al+UqHU7gnQq2nHRAE7CIewXC9cwuq"U
          aws configure set region "ap-northeast-1"

      - name: List S3 buckets
        run: |
          $ BUCKET_COUNT=$(aws s3 ls | wc -l)
          echo "S3バケットの数は$BUCKET_COUNTです"
